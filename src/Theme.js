
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    mode: 'light',
    primary: {
      main: '#F9B572',
    },
    secondary: {
      main: '#748E63',
    },
    background: {
      paper: '#FAF8ED',
    },
  },
})

export default theme;